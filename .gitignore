# Runtime state — never part of the deployed repo, lives under /home/pi/sitestream on the device
*.log
config.env
schedule.json
.manifest_hash
.schedule_updated

# Built pi-portal bundle (standalone-mode local portal) — source lives in
# the main SiteStream monorepo's packages/pi-portal; this is just the build
# output copied in here for packaging into a release tarball, regenerated
# each time, not checked in.
pi-portal/
